#!/bin/sh
# Safe sed insert
KEY=$(echo "$1" | tr -cd 'a-zA-Z0-9_')
VAL=$(echo "$2" | tr -cd 'a-zA-Z0-9._:/ -')
FILE="$3"
[ -z "$KEY" -o -z "$FILE" -o ! -f "$FILE" ] && exit 1
sed -i "1i${KEY}=\"${VAL}\";" "$FILE"
