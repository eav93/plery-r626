#!/bin/sh
# Safe cron every-minute entry
CMD=$(echo "$1" | tr -cd 'a-zA-Z0-9 /_.-')
FILE=$(echo "$2" | tr -cd 'a-zA-Z0-9/_.-')
[ -z "$CMD" -o -z "$FILE" ] && exit 1
echo "*/1 * * * * $CMD" >> "$FILE"
